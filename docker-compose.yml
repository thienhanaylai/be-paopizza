services:
  api:
    container_name: paopizza-api
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - '${PORT:-4000}:${PORT:-4000}'
    restart: unless-stopped
