services:
  api:
    container_name: paopizza-api
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
    ports:
      - '${PORT:-4000}:${PORT:-4000}'
    restart: unless-stopped
